// You Can't Code Under Pressure #1
function doubleInteger(i) {
    // i will be an integer. Double it and return it.
    return i * 2;
  }

// Alternative Method
function doubleInteger(i) {
    // i will be an integer. Double it and return it.
    return i + i;
  }